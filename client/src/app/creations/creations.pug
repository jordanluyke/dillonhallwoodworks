.container
    .row
        .col-sm-10.mx-auto
            .row.d-flex.align-items-start
                .col-sm-4(*ngFor="let url of igUrls")
                    a.creation([href]="url" target="_blank" rel="noreferrer")
                        img([src]="url + '/media/?size=m'")
